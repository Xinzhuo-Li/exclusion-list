# Caddy reverse proxy for Medicaid Exclusion List web app.
# Copy to /etc/caddy/Caddyfile or include as a site block.

your.server.example {
    reverse_proxy 127.0.0.1:8000
    handle_path /static/* {
        root * /home/USER/exclusion-list/web/staticfiles
        file_server
    }
}
